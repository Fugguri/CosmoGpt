
version: "3.10"

services:
  telegram-bot:
    container_name: CosmoBot
    working_dir: /.
    restart: always
    build:
        context: .
        dockerfile: Dockerfile
    command: sh -c "python main.py" "python server.py"